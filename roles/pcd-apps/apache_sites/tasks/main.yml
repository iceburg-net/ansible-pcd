---
##
#  apache_sites tasks 
#

#@todo review skip_apache_sites_tasks is not defined

- debug: msg="apache_sites application"

- { include: "normalize.yml", tags: ['prepare','deploy'] }
#- debug: msg="{{ apache_sites_normalized }}"  

- { include: prepare.yml, tags: ['prepare'], when: skip_tasks is not defined or not skip_tasks }
- { include: deploy.yml, tags: ['deploy'], sudo: True, sudo_user: "{{ apache_sites_user }}", when: skip_tasks is not defined or not skip_tasks }
