---
##
#  pcd-apps/backup tasks 
#

- { include: prepare.yml, tags: ['prepare'], when: skip_tasks is not defined or not skip_tasks }
- { include: deploy.yml, tags: ['deploy'], sudo: True, sudo_user: "{{ backup_user }}", when: skip_tasks is not defined or not skip_tasks }
